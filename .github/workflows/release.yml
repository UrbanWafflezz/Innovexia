name: Build and Release APK

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v1.2.3, etc.

permissions:
  contents: write  # Required to create releases

jobs:
  build:
    name: Build Signed APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Decode google-services.json from secrets
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -z "$GOOGLE_SERVICES_JSON" ]; then
            echo "ERROR: GOOGLE_SERVICES_JSON secret is not set!"
            echo "Please add the secret in GitHub repository Settings > Secrets and variables > Actions"
            exit 1
          fi
          echo "$GOOGLE_SERVICES_JSON" | base64 -d > app/google-services.json
          if [ ! -f app/google-services.json ]; then
            echo "ERROR: Failed to create google-services.json"
            exit 1
          fi
          echo "✓ google-services.json created successfully"
          ls -lh app/google-services.json

      - name: Decode keystore from secrets
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 -d > $GITHUB_WORKSPACE/keystore.jks

      - name: Create local.properties
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          echo "GEMINI_API_KEY=$GEMINI_API_KEY" > local.properties

      - name: Build Release APK
        env:
          KEYSTORE_FILE: ${{ github.workspace }}/keystore.jks
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew assembleRelease --stacktrace

      - name: Get version name
        id: version
        run: |
          VERSION=$(echo ${{ github.ref_name }} | sed 's/^v//')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: List built APKs (debug)
        run: |
          echo "=== Listing all APK files ==="
          find app/build/outputs/apk -name "*.apk" -ls
          echo "=== Release APK directory ==="
          ls -lah app/build/outputs/apk/release/ || echo "Release directory not found"

      - name: Rename APK
        run: |
          mkdir -p release-apks
          # Only copy the release APK, not debug
          cp app/build/outputs/apk/release/Innovexia-v${{ steps.version.outputs.version }}-release.apk release-apks/Innovexia-${{ steps.version.outputs.version }}.apk || \
          find app/build/outputs/apk/release -name "*-release.apk" -not -name "*-debug*" -exec cp {} release-apks/Innovexia-${{ steps.version.outputs.version }}.apk \;

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release-apks/*.apk
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}
          body: |
            ## Download

            Download the APK below and install it on your Android device.

            **Installation Instructions:**
            1. Download the APK file
            2. Enable "Install from Unknown Sources" in your Android settings
            3. Open the downloaded APK to install
            4. You may need to grant installation permissions

            ---

            ## What's New in This Release

            ### Fixes
            - **Security Enhancement**: Removed HTTP logging interceptor from release builds to prevent sensitive data exposure in production
            - **Improved Performance**: Release builds now run faster without debug logging overhead
            - **Build Optimization**: Cleaned up build configuration for better APK size and performance

            ### Notes
            - This is a **signed release APK** (not a debug build)
            - All releases are automatically built and signed via GitHub Actions
            - APK is fully minified and optimized with ProGuard

            ---

            See the full changelog below for all changes in this release.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: release-apks/*.apk
          retention-days: 90
