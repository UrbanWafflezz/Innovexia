<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory for downloaded APK files -->
    <cache-path name="apk_cache" path="apk/" />

    <!-- Cache directory for camera images -->
    <cache-path name="camera_images" path="." />

    <!-- External cache directory (fallback) -->
    <external-cache-path name="external_apk_cache" path="apk/" />
</paths>
